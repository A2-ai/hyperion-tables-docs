---
title: "Apply summary specification to lineage tree"
description: "Filters models, loads summaries, and prepares data for table generation."
pagefind: true
---

## Description

Filters models, loads summaries, and prepares data for table generation.

## Usage

```r
apply_summary_spec(tree, spec = SummarySpec())
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr>
<th>Argument</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>tree</code></td>
<td>A hyperion_nonmem_tree object from <code>get_model_lineage()</code></td>
</tr>
<tr>
<td><code>spec</code></td>
<td>A SummarySpec object</td>
</tr>
</tbody>
</table>
</div>

## Value

Data frame with summary_spec attribute, ready for make_summary_table()
