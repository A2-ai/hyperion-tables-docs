---
title: "Build parameter table"
description: "Creates a formatted table from parameter data."
pagefind: true
---

## Description

Creates a formatted table from parameter data. Supports multiple output formats: gt (default), flextable, or the intermediate HyperionTable object.

## Usage

```r
make_parameter_table(params, output = c("gt", "flextable", "data"))
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr>
<th>Argument</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>params</code></td>
<td>Parameter data frame from <code>get_parameters()</code> or enriched via <code>apply_table_spec()</code></td>
</tr>
<tr>
<td><code>output</code></td>
<td>Output format: "gt" (default), "flextable", or "data" for the intermediate HyperionTable object.</td>
</tr>
</tbody>
</table>
</div>

## Value

A gt table, flextable, or HyperionTable object depending on `output`
