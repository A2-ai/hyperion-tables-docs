---
title: "Set section rules for a spec"
description: "Controls how rows are grouped into sections. Pass formula expressions where the LHS is a condition and the RHS is the section label."
pagefind: true
---

## Description

Controls how rows are grouped into sections. Pass formula expressions
where the LHS is a condition and the RHS is the section label.

## Details

For TableSpec, rules are evaluated against parameter columns
(e.g., `kind == "THETA" ~ "Structural Parameters"`).

For SummarySpec, rules are evaluated row-by-row against summary columns
including `tags` (e.g., `"base" %in% tags ~ "Base Models"`).

Built-in methods for `TableSpec` and `SummarySpec` interpret unnamed
arguments in `...` as section rule formulas. Named arguments in `...` are
ignored with a warning.






## Usage

```r
set_spec_sections(spec, ..., overwrite = FALSE)
```

## Arguments

<div class="arg-table">
<table>
<thead>
<tr><th>Argument</th><th>Description</th></tr>
</thead>
<tbody>
<tr>
<td><code>spec</code></td>
<td>A TableSpec or SummarySpec object</td>
</tr>
<tr>
<td><code>...</code></td>
<td>Additional arguments passed to methods.</td>
</tr>
<tr>
<td><code>overwrite</code></td>
<td>If FALSE (default), append to existing rules. If TRUE, replace all existing rules.</td>
</tr>
</tbody>
</table>
</div>


## Value

Modified spec




## Examples

```r
spec <- TableSpec() |>
  set_spec_sections(
    kind == "THETA" ~ "Structural",
    kind == "OMEGA" ~ "IIV"
  )
```

